{
  "name": "main-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel start:server start:web",
    "start:server": "cd Server && npm run start",
    "start:web": "cd Web-app && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "axios": "^1.8.4"
  }
}
